<br /><br />
<h1 class="text-center"> Voici votre panier !</h1>
<br />
<br />
<div class="card-body">

	<% if @cart.cart_items.first %>
  <% @cart.cart_items.each do |cart_item|%>
    <div class="row">
      <div class="col-12 col-sm-12 col-md-2 text-center">
        <img class="img-responsive" src="http://placehold.it/120x80" alt="prewiew" width="120" height="80">
      </div>
      <div class="col-12 text-sm-center col-sm-12 text-md-left col-md-6">
        <h4 class="product-name"><strong> <%= cart_item.item.title %> </strong></h4>
        <small> <%= cart_item.item.description %></small>
      </div>
      <div class="col-12 col-sm-12 text-sm-center col-md-4 text-md-right row">
        <div class="col-3 col-sm-3 col-md-6 text-md-right" style="padding-top: 5px">
          <h6><strong> Price : <%= cart_item.item.price.to_i %> €</strong> </h6>
        </div>

        <div class="col-3 col-sm-2 col-md-2 text-right">
					<%= link_to "Supprimer du Panier", item_cart_item_path(cart_item.item.id, cart_item.id), method: "delete", class: "btn btn-outline-danger" %>
        </div>
      </div>
    </div>
    <hr>
  <% end %>
  <div class="text-right mr-5 ml-5">
  	<h5 class=" ">Prix Total :  &nbsp;    <span style = "color:grey"><%= @price_sum %>  euros</h5> </span><br/><br/>
		<%= link_to "PASSER COMMANDE STRIPE", new_charge_path, class: "btn btn-secondary btn-block  d-flex align-items-center justify-content-center", style: "height:50px" %>
	
	<% else %>

		<p>Votre panier ne contient aucune photo...</p>

	<% end %>
  </div>
</div>
